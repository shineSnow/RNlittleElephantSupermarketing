import {Button, Layout, Text} from '@ui-kitten/components';

const SortScreen = () => {
  return (
    <Layout style={{flex: 1}}>
      <Text category="h1">Sort pages</Text>
    </Layout>
  );
};
export default SortScreen;
